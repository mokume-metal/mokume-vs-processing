<!--
SPDX-FileCopyrightText: 2026 mokume-metal
SPDX-License-Identifier: MIT
-->

# AGENTS.md

**規約の正典は [mokume の AGENTS.md](https://github.com/mokume-metal/mokume/blob/main/AGENTS.md) である。**
Issue の起こし方・分類・コメントの置き場と署名・コミットと PR の書式・機構を足す順序 (実害 → Issue → 機構)
は、あちらを読んで同じように振る舞う。**ここには写しを置かない** (mokume ADR-0026 決定 3)。

このファイルが持つのは、**このリポジトリだけで違うこと**だけである。

## 違うこと

| | mokume | ここ |
| --- | --- | --- |
| 何のためのリポジトリか | ライブラリ本体 | **外の人に見せる事例集。** 数字は mokume を選ぶ根拠として読まれる |
| 設計判断の正典 | `docs/decisions/` の ADR | **同じく持つ** — ただし扱うのは比べ方だけ ([ADR-0000](docs/decisions/0000-record-decisions.md))。外のパッケージが ADR を持たない (ADR-0026 第 3 段) のから外れる |
| 検査 | `make ci-check` | `make ci-check` (**事例の並び・ハーネス・reuse lint の 3 本**)。足すのは実害が出てから |
| 描画の証跡 | 描画 PR に必須 | **求めない** (ADR-0026 第 3 段)。代わりに、スケッチを触った PR には計測の表を貼る |
| ルールセットと App | 承認 1 を求めるルールセット + App | **承認 0 のルールセットだけ** (PR 必須・必須チェック・署名)。承認を求めないので App は要らない (ADR-0026 第 2 段。承認が要る変更が育ったら 2 つ同時に入れる) |
| 版の張り方 | — | mokume は **`exact`** で固定する。版を上げる PR で全事例を測り直して表を貼り替える |

## 公平さ — 「その比較はずるい」と言われないために

**このリポジトリの価値は、数字が信用できることだけにある。** 読み手は mokume の側が作った比較だと知っていて、疑ってかかる。スケッチを 1 本開いて「Processing 側だけ遅い書き方をしている」「条件が片側に有利だ」と 1 か所でも見つければ、他の事例の数字まで読まれなくなる。

だから目標は **mokume を勝たせることではなく、Processing に詳しい人が読んでも文句の付けようがない比較を置くこと**である。mokume が負ける結果が出たら、それも載せる。負けを隠した比較は、勝ちの数字の信用も削る。

各項の根拠と具体的な条件は [ADR-0001](docs/decisions/0001-fair-comparison.md) にある。ここに置くのは、スケッチと PR を書くときの判断の基準である。

### 書き方 — その道具の利用者がふつうに書く形にする

- **両側とも「その道具を使う人が最初に書く、一般的な書き方」で書く。** 基準は公式のリファレンス・Examples・チュートリアルに載っている形である。Processing なら [processing.org の Examples](https://processing.org/examples/) や Reference の書き方、mokume なら README とリファレンスの書き方
- **片側だけを最適化しない。** Processing の `PShape` のまとめ描き・GLSL・低水準の OpenGL、mokume の `shape(_:at: [Placement])`・GPU の粒子系といった速く書く技法は、一般的な事例では使わない。使うなら**両側に同じ水準の最適化を施した別の事例**として足し、一般的な書き方の事例と並べる (ADR-0001 決定 4)
- **Processing 側をわざと古い書き方・遅い書き方にしない。** Java として素直な書き方 (配列・`for`・組み込み関数) にとどめ、Processing の既定の書き方から外れるときはコメントで理由を書く
- 迷ったら、**Processing 側が速くなるほうを選ぶ**

### 対称性 — 同じ仕事をさせる

- **同じアルゴリズム・同じデータ構造・同じ描画プリミティブ・同じ呼び出し回数**にする。1 フレームに `circle()` を N 回呼ぶなら、両側とも N 回呼ぶ
- **2 つのファイルを並べて、行単位で対応が取れるように書く。** 変数名・処理の順序・関数の分け方をそろえ、言語の違いで避けられない差だけを残す
- **避けられない差は事例の README に列挙する** (言語や API の違いで同じ書き方にできない箇所・既定値の違い)。書かれていない差は、読み手には隠したように見える
- **描いた絵が同じであることを確かめる。** 両側のスクリーンショットを PR に並べて貼る。アンチエイリアス・色の扱い・線の太さなど描画品質の既定が違うなら、揃えるか、揃えられない差として README に書く。**描く量や品質を落として速く見せたと言われる余地を残さない**
- 計測のためのコードは `Recorder` に閉じ込め、スケッチの本体に混ぜない。`Recorder` は両側で同じ振る舞いにする (harness/README.md の取り決め)

### 条件 — 片側だけ設定を変えない

- 大きさ・pixelDensity・目標の fps・暖機と計測の秒数は ADR-0001 決定 3 の値に揃える。**事例ごと・片側だけの変更はしない**
- 既定値を変えるときは両側で同じ意味の設定を明示し、理由を事例の README に書く。**片側だけの設定変更は、それだけで偏りを疑われる**
- 両側とも**その時点の最新の安定版**を使う。mokume は release ビルド、Processing は GPU の経路 (P2D) を使う。debug の数字は載せない
- 同じ機械・同じ状態で測る (電源に繋ぐ・他のアプリを閉じる・窓を前面に置く)。両側を交互に回し、片側だけが温まった状態や冷えた状態で測らない
- **1 回だけの数字を載せない。** 繰り返して、ばらつきと合わせて示す

### 載せ方 — 読み手が自分で確かめられるようにする

- **表には harness が出す最後の 1 行 (機械・macOS・両側の版・条件) を必ず添える**
- **mokume に不利な事例や負荷の段も隠さない。** 事例の選び方が mokume の得意な題材に偏らないようにし、その題材を選んだ理由を README に書く
- 手元で再現できる手順を README に置く。数字を読んだ人が自分の Mac で同じコマンドを打てば同じ比較を回せる状態を保つ
- **比べ方を変えたら、全事例を測り直す。** 条件の違う数字を 1 つの表に混ぜない。条件そのものを変えるなら ADR を改訂する
- Processing に詳しい人からの「この書き方は不公平だ」という指摘は、最優先で受け止める。指摘が正しければ直して測り直し、その経緯を Issue に残す

## 踏みやすいこと

- mokume 側のパッケージを `mokume/` の直下に置かない。SwiftPM の識別名がフォルダ名から付き、依存の `mokume` と衝突する (`harness/cases.py` の冒頭)
- Processing 側で `record` を変数名にしない。Java 16 からの予約語で、前処理が構文エラーにする
- **計測は CI では回らない。** 窓と GPU が要る。CI の緑は「数字が正しい」を意味しない
- mokume の不具合を見つけたら mokume 側へ起票する。ここに回避策を書き足さない
